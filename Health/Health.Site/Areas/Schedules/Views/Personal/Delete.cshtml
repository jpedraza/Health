@using Health.Site.Areas.Schedules.Models
@model PersonalScheduleForm

@{
    ViewBag.Title = "Удаление расписания";
}

<h2>@Model.Message
    <p>
        @Html.RouteLink("Да", "Schedules_Delete", new {id = Model.PersonalSchedule.Id, confirm = true})
        @Html.RouteLink("Нет", "Schedules_Delete", new {id = Model.PersonalSchedule.Id, confirm = false})
    </p>
</h2>

@Html.Partial("_PersonalScheduleDisplay")