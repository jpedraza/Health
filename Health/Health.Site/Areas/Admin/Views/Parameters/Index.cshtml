
@{
    ViewBag.Title = "Редакирование параметров";
}

<h3>Проект редактирования параметров здоровья человека</h3>
<p>Здесь можно создать новые параметры, редактировать и удалять существующие.</p>
@{
    <p>@Html.RouteLink("Просмотреть все параметры", new { area = "Admin", controller = "Parameters", action = "ShowAll" }) </p>
    <p>@Html.RouteLink("Добавить новый параметр", new { area = "Admin", controller = "Parameters", action = "AddParameter" })</p>
    <p>@Html.RouteLink("Редактирование и удаление параметров", new { area = "Admin", controller = "Parameters", action = "StartEditing" })</p>
}
